[build-system]
requires = ["setuptools>=64", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "rsktools"
version = "0.1.0"
description = "Tools for reading Rockland Scientific microprofiler data and computing turbulent dissipation rates"
requires-python = ">=3.10"
license = "GPL-3.0-or-later"
dependencies = [
    "numpy",
    "netCDF4",
    "scipy",
    "xarray",
]

[project.optional-dependencies]
dev = [
    "pytest",
]

[project.scripts]
p2nc = "rsktools.cli:p2nc"
pinfo = "rsktools.cli:pinfo"
p2prof = "rsktools.cli:p2prof"
p2eps = "rsktools.cli:p2eps"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
